--[[
    phauka.win - GUI Library
    Keybind: J to toggle
--]]

--[[
    API METHODS
--]]
```lua
phauka:AddFrame(userId?)

phauka:AddButton(name, parent?)

phauka:AddSwitch(parent, text, callback)

phauka:AddSlider(parent, text, min, max, default, callback)

phauka:AddLabel(parent, text, color?)

phauka:AddActionButton(parent, text, callback)

phauka:Notify(config)

phauka:Destroy()
```
